# Real-Time 3D Gaussian Splatting - Python Dependencies

# Core Python Dependencies
pyyaml>=6.0.1
python-dotenv>=1.0.0

# Video Processing
ffmpeg-python>=0.2.0
opencv-python>=4.8.0
Pillow>=10.0.0

# Deep Learning & 3D Reconstruction
# NOTE: Install PyTorch with CUDA 12.1 for GPU support:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# CPU-only wheels will NOT work with GPU features
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0
scipy>=1.11.0

# Gaussian Splatting & 3D Vision
# NOTE: gsplat 1.5.3+ uses JIT CUDA compilation (requires MSVC + CUDA toolkit)
# Fallback: PyTorch rasterization works without CUDA compilation
gsplat>=1.5.0
trimesh>=4.0.0
plyfile>=1.0.0
roma>=1.3.0  # Rotation and transformation utilities

# DUST3r/MASt3r Dependencies
# Note: These may require installation from source
# MASt3r: https://github.com/naver/mast3r
# DUST3r: https://github.com/naver/dust3r
einops>=0.7.0
huggingface-hub>=0.19.0
# gradio>=4.0.0  # Removed: Not needed for core demo, huge download

# 3D Processing
# open3d>=0.18.0  # Removed: Not needed for core demo, compilation issues on Windows
# pytorch3d; sys_platform != 'win32'  # Removed: Causes issues on Windows pip installs

# Azure Cloud Storage
azure-storage-blob>=12.19.0
azure-identity>=1.15.0

# Utilities
tqdm>=4.66.0
watchdog>=3.0.0  # File system monitoring
psutil>=5.9.0  # System monitoring
colorama>=0.4.6  # Colored console output (Windows)

# Logging & Monitoring
loguru>=0.7.0

# Optional: RTMP Server Control (if managing MediaMTX via Python)
requests>=2.31.0

# Development Dependencies (Optional)
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
flake8>=6.1.0
mypy>=1.5.0

# Note: Manual Installation Steps (COMPLETED)
# ============================================
# ✓ 1. CUDA Toolkit 12.9 detected (driver supports CUDA 12.1 runtime)
# ✓ 2. PyTorch 2.5.1 with CUDA 12.1 installed:
#       pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# 
# ✓ 3. gsplat 1.5.3 installed (JIT CUDA compilation):
#       pip install gsplat
#    NOTE: Requires MSVC compiler for CUDA JIT compilation
#          Set up environment: "C:\Program Files\Microsoft Visual Studio\18\Community\VC\Auxiliary\Build\vcvarsall.bat" x64
#          Known issue: VS 2026 not officially supported by CUDA 12.9 (use PyTorch fallback if needed)
#
# ✓ 4. MASt3r cloned to tools/mast3r/:
#       cd tools && git clone --recursive https://github.com/naver/mast3r.git
#       cd mast3r && pip install -r requirements.txt
#       cd dust3r && pip install -r requirements.txt
#    Model weights auto-downloaded on first use (~2.75GB from HuggingFace)
#
# 5. For PyTorch3D on Windows (optional, only if needed):
#    - Requires Visual Studio Build Tools
#    - May need pre-built wheels from:
#      https://github.com/facebookresearch/pytorch3d/releases
#
# 6. Download MediaMTX from:
#    https://github.com/bluenviron/mediamtx/releases
#    Extract to project root
